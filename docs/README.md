# MODULES

msd = METADATA SHOULD DIE

force-tor: force your network to route through Tor


zero-trust: starts Zero Trust Service (every network packet is treated as unsafe first then analyzed, not recommended while using Tor)

isolate-file: isolates a file (generally, an executable) to run in a isolated container. [Docker required] 


```core/modules/msd.py``` 